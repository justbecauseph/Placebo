# Loom reads the platform from this property, not from architectury { neoForge() }.
# Without it the subproject stays on the default FABRIC platform and the `neoForge`
# dependency configuration is never created.
loom.platform = neoforge
